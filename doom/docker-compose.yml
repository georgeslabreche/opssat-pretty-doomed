services:
  doom:
    build: .
    container_name: doom
    volumes:
      - ./src:/app/src
      - ./demos:/app/demos
      - ./Makefile:/app/Makefile
      - ./run:/app/run
      - ./toGround:/app/toGround
    working_dir: /app
    tty: true
