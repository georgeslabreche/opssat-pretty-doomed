sudo apt-get install qemu-user-static
tar zxvf exp_env.tar.gz -C exp_env
sudo cp /etc/resolv.conf exp_env/etc/
sudo chroot exp_env /bin/sh

# in that shell
apk add gcc musl-dev

# i also added this
apk add make